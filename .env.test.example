# Copy to env/.env.test and fill in (never commit - env/ is gitignored):
SMOKE_EMAIL=you@example.com
SMOKE_PASSWORD=your-password
# Optional override, defaults to http://localhost:4444
# SMOKE_BASE_URL=http://localhost:4444
